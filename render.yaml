services:
  - type: web
    name: anilist-server
    env: docker
    dockerContext: server
    dockerfilePath: server/Dockerfile
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGIN
        value: "*"
